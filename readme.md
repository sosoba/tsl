# Typescript loader for NodeJS 26

Allows you to run in NodeJS Typescript code that requires transpilatione (enums, constructor properties)

## Ussage:

```bash
npm i -D @sosoba/tsl

node --import=@sosoba/tsl ./appliaction.ts
```
